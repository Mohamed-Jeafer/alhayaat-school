PDFs in this folder must match `requirements.forms[].file` in `src/content/admissions.json`.

Current files (spaces are OK — the app builds URLs with encodeURIComponent):

  Registration Form.pdf
  PAD Agreement.pdf
  Financial Agreement.pdf
  Parental Agreement.pdf
  Liability Waiver.pdf

Production: App Service gets NEXT_PUBLIC_REGISTRATION_FORMS_BASE_URL from app-service.bicep (and CI sets the same value at build). Example (prod, no trailing slash):

  https://alhayaatassetsprod.blob.core.windows.net/school-registration

Local: set NEXT_PUBLIC_REGISTRATION_FORMS_BASE_URL in .env.local to the same pattern if you want blob links instead of /assets/registration/.

When Azure CDN (or Front Door) is enabled, switch that value to the CDN endpoint + same path segment; filenames in admissions.json stay the same.
